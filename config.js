/*
 * GQMA Site Configuration
 *
 * This file is safe to commit — it contains NO secrets.
 * For sensitive values (email, API keys), create config.local.js
 * which is gitignored. See .env.example for the template.
 */

window.GQMA_CONFIG = {
  // Contact info is loaded from config.local.js (gitignored)
  // to keep personal details out of the repo.
  email: null,
  github: null,
  linkedin: null,
};
